One discrepancy surfaced that the plan needs to address: routes.ts (line 68-72) already defines a generic bh/:bhId/:zone? route → GalacticCenterPage.vue, separate from /galactic-center (Sgr A* only). The draft's framing — "no route or scene exists today" for bh-orbital zones — is only fully true for Sgr A*; other catalogued black holes already route somewhere. I haven't yet checked whether that generic route renders a real scene or also falls into a fallback, so I can't tell yet whether claimSettlement()'s redirect target needs to generalize beyond the hardcoded Sgr-A* string. Flagging this now rather than glossing over it.
