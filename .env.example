# ── Supabase ─────────────────────────────────────────────────────────────────
# Create a free project at supabase.com, then copy these from:
#   Project Settings → API → Project URL and anon/public key
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON=your-anon-public-key-here

# ── Algorand (existing) ───────────────────────────────────────────────────────
# VITE_ALGO_NODE=https://mainnet-api.algonode.cloud
# VITE_ALGO_INDEXER=https://mainnet-idx.algonode.cloud

# ── Support inbox (workers/support-inbox) ───────────────────────────────────
# URL of the deployed Cloudflare Worker that receives contact-form submissions
# (defaults to its *.workers.dev subdomain — see workers/support-inbox/README.md).
# VITE_SUPPORT_WORKER_URL=https://vercel-html-exotopia-org.ecocommunity.workers.dev
#
# Public Turnstile site key (safe to expose client-side; the matching secret
# key lives only in the worker). Get both from the Cloudflare dashboard →
# Turnstile → Add site.
# VITE_TURNSTILE_SITE_KEY=your-turnstile-site-key-here
#
# For local dev / automated testing (Playwright etc.), use one of Cloudflare's
# documented TEST sitekeys instead of a real one — real Turnstile challenges
# flag automated browsers as bots and break test runs. Pair with the matching
# TEST secret key in workers/support-inbox/.dev.vars (see that file). Never
# use a test key/sitekey pair in production.
#
#   Test sitekey                          | Behavior            | Widget
#   1x00000000000000000000AA              | Always passes       | Visible
#   2x00000000000000000000AB              | Always fails        | Visible
#   1x00000000000000000000BB              | Always passes       | Invisible
#   2x00000000000000000000BB              | Always fails        | Invisible
#   3x00000000000000000000FF              | Forces interactive  | Visible
#
# VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA

# ── IPFS pinning (Pinata v3) ─────────────────────────────────────────────────
# Create a SCOPED key at pinata.cloud → API Keys → New Key. Under
# Permissions → Files, enable "Write" only — that's all the v3 upload call
# in src/lib/ipfs-pinning.ts needs (POST uploads.pinata.cloud/v3/files).
# Leave Groups/Gateways/Analytics off, and do NOT use an Admin key: this is
# a VITE_ var, so it ships in the public client bundle and is readable by
# anyone who opens devtools on the deployed site.
# VITE_PINATA_JWT=your-scoped-pinata-jwt-here
